In this challenge you'll trigger the PrDigest workflow, poll until it completes, and read the ranked digest the agents produced.

Make sure the application from the previous challenge is still running (`aspire run` in the *Aspire Terminal*). If you restarted the sandbox, run it again first.
