.venv/
__pycache__/
*.pyc
# Robot Framework outputs
output.xml
log.html
report.html
/results/
# Stray file from Run Process stdout=PIPE (Robot treats PIPE as a filename, not a
# sentinel); harmless, but keep it out of the tree.
PIPE
